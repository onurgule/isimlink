name: isimlink
description: tasarimodev
version: 1.1.0+2

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.5
  webview_flutter: ^1.0.7
  url_launcher: ^5.7.10
  flutter_webview_plugin: ^0.3.11
  flutter_inappwebview: ^4.0.0
  shared_preferences: ^0.5.12+4
  http: ^0.12.2
  share: ^0.6.5+4
  font_awesome_flutter: ^8.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
